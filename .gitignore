# 로컬 머신 설정 (저장소에 올리지 않음)
.claude/settings.local.json

# 로컬 환경변수 / API 키 (저장소에 올리지 않음)
.env
.env.*
.vercel

# OS/에디터 부산물
.DS_Store
Thumbs.db

# 참고용 클론 프로젝트 (최종 배포에서 제외)
website-clone-for-ai/

# npm 의존성 (langfuse SDK 설치로 생김)
node_modules/

# 설정 예시 파일은 추적한다 (.env.* 패턴의 예외)
!.env.example
